% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sobol_report}
\alias{sobol_report}
\title{Summarise a Sobol sensitivity analysis}
\usage{
sobol_report(x, n = 500, cor_to = NULL, plots = TRUE, top_n = 5, ...)
}
\arguments{
\item{x}{A tidy Sobol data frame (output, parameter, first_order, total) or a
`"compartmentalFit"`/`"compartmentalModel"` object.}

\item{n}{Base sample size when computing from a fit/model.}

\item{cor_to}{Optional parameter name to correlate others against.}

\item{plots}{If `TRUE`, build a total-index heatmap.}

\item{top_n}{Number of top parameters to report per stream.}

\item{...}{Passed to [sobol_streams()] when computing.}
}
\value{
Invisibly, a list with the table, stream sums, parameter ranking,
  not-converged flags, range/NA diagnostics, and (if `plots`) a heatmap.
}
\description{
Reports validity, per-stream top parameters, interaction structure, and a
global parameter ranking from a Sobol analysis. Accepts a precomputed tidy
table or a fit/model (computed via [sobol_streams()]).
}
\examples{
\dontrun{
sobol_report(fit, n = 500)            # compute from a fit, or:
sob <- sobol_streams(fit, n = 500)
r   <- sobol_report(sob)              # report a precomputed table
}
}
