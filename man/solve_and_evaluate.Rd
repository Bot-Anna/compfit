% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{solve_and_evaluate}
\alias{solve_and_evaluate}
\title{Solve the ODE and evaluate formulas}
\usage{
solve_and_evaluate(fit, initial_state, parms, data_dummy = NULL)
}
\arguments{
\item{fit}{A `"compartmentalFit"` object.}

\item{initial_state}{Named numeric vector of initial compartment values.}

\item{parms}{Named numeric vector of parameters.}

\item{data_dummy}{Optional dummy-data data frame; `NULL` evaluates only the
data streams.}
}
\value{
A list with `sir_out` (trajectory) and `evaluation` (formula columns).
}
\description{
Solves the model at a given `(initial_state, parms)` and evaluates every data
(and optional dummy) formula on the resulting trajectory. Self-sufficient:
reads the time grid, solver settings and formulas from the `fit` object.
}
\examples{
\dontrun{
# fit from fitCompartmentalModel(); evaluate at the fitted point
p  <- get_point(fit)
ev <- solve_and_evaluate(fit, p$initial_state, p$parms)
head(ev$evaluation)
}
}
