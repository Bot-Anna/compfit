% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_fit}
\alias{plot_fit}
\title{Plot a compartmental fit}
\usage{
plot_fit(
  fit,
  bands = TRUE,
  band_type = c("predictive", "mean", "both", "spaghetti"),
  n_draws = 200,
  n_rep = 5,
  spaghetti_draws = 100,
  spaghetti_predictive = FALSE,
  base_size = 11,
  ncol = NULL,
  data_dummy = NULL,
  palette = NULL
)
}
\arguments{
\item{fit}{A `"compartmentalFit"` object.}

\item{bands}{Whether to draw uncertainty bands (Bayesian fits).}

\item{band_type}{Band style: predictive, mean, both, or spaghetti.}

\item{n_draws}{Posterior draws used for band construction.}

\item{n_rep}{Replicates per draw for predictive bands.}

\item{spaghetti_draws}{Number of spaghetti trajectories.}

\item{spaghetti_predictive}{If `TRUE`, spaghetti lines include observation
noise.}

\item{base_size}{Base font size.}

\item{ncol}{Number of facet columns (default: automatic).}

\item{data_dummy}{Optional dummy-data data frame to overlay.}

\item{palette}{Colour scheme: `"okabe"` (default, colour-blind-safe) or
`"grey"`/`"grayscale"` for monochrome output; also accepts a custom palette
list (see [cfit_palette]). `NULL` honours `options(compfit.palette=)`.}
}
\value{
A list with per-stream `plots` and (if patchwork is available) a
  combined `grid`, plus the plotting `code`.
}
\description{
One panel per data stream: observed points, the fitted trajectory, and
(optionally) uncertainty bands or spaghetti draws for Bayesian fits.
}
\examples{
\dontrun{
res <- plot_fit(fit, band_type = "both")       # fit from fitCompartmentalModel()
res$grid
res_bw <- plot_fit(fit, palette = "grey")      # monochrome
options(compfit.palette = "grey")              # ... or switch every plot
}
}
