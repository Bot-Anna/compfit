% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{read_data_file}
\alias{read_data_file}
\title{Read a data file (csv or xlsx)}
\usage{
read_data_file(path, text_cols = FALSE, ...)
}
\arguments{
\item{path}{Path to a `.csv` or `.xlsx` file.}

\item{text_cols}{If `TRUE`, read all columns as character.}

\item{...}{Passed to the underlying reader.}
}
\value{
A data frame.
}
\description{
Reads a `.csv` or `.xlsx` data file, optionally forcing every column to be
read as text so numbers stored as text round-trip unchanged.
}
\examples{
f <- system.file("extdata", "minimal", "modelParams.csv", package = "compfit")
read_data_file(f, text_cols = TRUE)
}
